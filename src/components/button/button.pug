mixin _wrapper(rounded)
  if rounded
    .button-wrapper
      block
  else
    block


//-TODO: Добавить src
mixin button(obj)

  -const buttonClassList = ["button", attributes.class];

    -
      if (obj.type === "a" || obj.type === "button") {
      let rounded = false;

        if (obj.modifiers) {
           if (obj.modifiers.rounded) buttonClassList.push("button_rounded"); rounded = true;
           if (obj.modifiers.primary) buttonClassList.push("button_primary")
           if (obj.modifiers.long) buttonClassList.push("button_long")
        }

    +_wrapper(rounded)
      #{obj.type}(class = buttonClassList) #{obj.text}
  -}